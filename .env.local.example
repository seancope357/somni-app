# Supabase Configuration
# Get these values from your Supabase project dashboard at https://supabase.com
# Go to Settings → API to find these values

# Project URL (found in Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous/Public Key (found in Settings → API → Project API keys → anon/public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (found in Settings → API → Project API keys → service_role)
# ⚠️ NEVER commit this to git - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Instructions:
# 1. Copy this file to .env.local (git will ignore it)
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Restart your development server
